<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AI Content Creator Manager</title>
  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background-color: #0e0e0e;
      color: #f1f1f1;
      display: flex;
      height: 100vh;
      overflow: hidden;
    }
    /* Sidebar */
    .sidebar {
      width: 250px;
      background: linear-gradient(180deg, #1a1a1a 0%, #000 100%);
      border-right: 2px solid #ffb300;
      display: flex;
      flex-direction: column;
      padding: 20px;
    }
    .sidebar h2 {
      color: #ffb300;
      margin-bottom: 30px;
      text-align: center;
      font-weight: 700;
    }
    .sidebar button {
      background: none;
      border: 1px solid #ffb300;
      color: #ffb300;
      padding: 12px;
      margin-bottom: 10px;
      border-radius: 8px;
      cursor: pointer;
      font-weight: 600;
      transition: all 0.2s ease-in-out;
    }
    .sidebar button:hover {
      background-color: #ffb300;
      color: #0e0e0e;
      transform: scale(1.03);
    }

    /* Main */
    .main {
      flex-grow: 1;
      padding: 20px;
      overflow-y: auto;
    }
    h1 {
      color: #ffb300;
      margin-bottom: 10px;
    }
    .panel {
      background-color: #1a1a1a;
      border: 1px solid #333;
      border-radius: 10px;
      padding: 20px;
      margin-bottom: 20px;
      box-shadow: 0 0 10px rgba(255, 179, 0, 0.2);
    }
    .panel h3 {
      color: #ffb300;
      margin-bottom: 15px;
    }
    .button-group {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
    }
    .button-group button {
      background-color: #ffb300;
      border: none;
      color: #000;
      font-weight: 700;
      border-radius: 8px;
      padding: 12px 18px;
      cursor: pointer;
      transition: all 0.2s ease;
    }
    .button-group button:hover {
      background-color: #ffd65c;
    }

    /* Analytics area */
    .analytics {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 20px;
    }
    .analytics .card {
      background-color: #121212;
      border: 1px solid #2a2a2a;
      border-radius: 8px;
      padding: 20px;
      text-align: center;
    }
    .analytics .card h4 {
      color: #ffb300;
      margin-bottom: 10px;
    }
    .analytics .card p {
      font-size: 18px;
      font-weight: 600;
    }

    footer {
      text-align: center;
      padding: 10px;
      border-top: 1px solid #333;
      color: #999;
      font-size: 13px;
    }
  </style>
</head>
<body>
  <div class="sidebar">
    <h2>Creator Hub</h2>
    <button onclick="switchPanel('channels')">🎬 Channels</button>
    <button onclick="switchPanel('automation')">⚡ Automation</button>
    <button onclick="switchPanel('assets')">🖼️ Assets</button>
    <button onclick="switchPanel('analytics')">📊 Analytics</button>
    <button onclick="switchPanel('settings')">⚙️ Settings</button>
  </div>

  <div class="main">
    <div id="channels" class="panel">
      <h1>Channel Management</h1>
      <h3>Create or Select a Channel</h3>
      <div class="button-group">
        <button onclick="createChannel('Horror Factory')">Horror Factory</button>
        <button onclick="createChannel('Documentaries HQ')">Documentaries HQ</button>
        <button onclick="createChannel('Kids Stories')">Kids Stories</button>
        <button onclick="createChannel('AI Shorts')">AI Shorts</button>
        <button onclick="createChannel('Educational AI')">Educational AI</button>
        <button onclick="createChannel('Mystery Files')">Mystery Files</button>
        <button onclick="createChannel('Fantasy Tales')">Fantasy Tales</button>
        <button onclick="createChannel('Science Explained')">Science Explained</button>
        <button onclick="createChannel('Motivation Studio')">Motivation Studio</button>
        <button onclick="createChannel('Daily News Digest')">Daily News Digest</button>
      </div>
      <p id="channelStatus"></p>
    </div>

    <div id="automation" class="panel" style="display:none;">
      <h1>Automation Control</h1>
      <div class="button-group">
        <button onclick="runAll()">▶️ Run All Channels</button>
        <button onclick="pauseAll()">⏸️ Pause All</button>
        <button onclick="schedule()">🕒 Schedule Uploads</button>
      </div>
    </div>

    <div id="assets" class="panel" style="display:none;">
      <h1>Asset Library</h1>
      <p>Upload or manage voice packs, sound effects, and image styles.</p>
      <div class="button-group">
        <button>Upload New Voice</button>
        <button>Upload Sound Effect</button>
        <button>Upload Background</button>
      </div>
    </div>

    <div id="analytics" class="panel" style="display:none;">
      <h1>Analytics Overview</h1>
      <div class="analytics">
        <div class="card"><h4>Total Videos</h4><p>1,257</p></div>
        <div class="card"><h4>Active Channels</h4><p>8</p></div>
        <div class="card"><h4>Monthly Views</h4><p>2.4M</p></div>
        <div class="card"><h4>Revenue</h4><p>$3,760</p></div>
      </div>
    </div>

    <div id="settings" class="panel" style="display:none;">
      <h1>Settings</h1>
      <p>Connect APIs, adjust upload frequency, manage cloud sync.</p>
      <div class="button-group">
        <button onclick="authorizeYouTube()">🔗 Authorize YouTube</button>
        <button onclick="syncGoogle()">☁️ Sync Google Sheets</button>
        <button onclick="saveSettings()">💾 Save Settings</button>
      </div>
    </div>
  </div>

  <footer>© 2025 AI Content Creator Manager — Powered by your genius.</footer>

  <script>
    function switchPanel(id) {
      document.querySelectorAll('.panel').forEach(panel => {
        panel.style.display = 'none';
      });
      document.getElementById(id).style.display = 'block';
    }

    function createChannel(name) {
      document.getElementById('channelStatus').innerText = `✅ Channel "${name}" is active and ready to create videos.`;
    }

    function runAll() {
      alert('🚀 All channels started — automation in progress.');
    }

    function pauseAll() {
      alert('⏸️ All channels paused.');
    }

    function schedule() {
      alert('🕒 Scheduling uploads automatically...');
    }

    function authorizeYouTube() {
      alert('🔗 YouTube OAuth authorization requested...');
    }

    function syncGoogle() {
      alert('☁️ Google Sheets sync started...');
    }

    function saveSettings() {
      alert('💾 Settings saved successfully.');
    }
  </script>
</body>
</html>
